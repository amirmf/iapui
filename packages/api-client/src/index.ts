/**
 * Public entry point for shared backend HTTP client code.
 *
 * Add Axios here when this package first creates the client; applications must
 * consume this package instead of owning duplicate generic HTTP clients.
 */
export {}
