# @iap/eslint-config

This package will hold shareable ESLint configuration once Storefront and Admin
have their own generated lint configurations. It intentionally exports nothing
yet so neither framework is selected or overridden prematurely.
