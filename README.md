# iap-ui

An intentionally blank pnpm frontend monorepo. Its workspace boundaries match the `ipos-ui` architecture, while product implementation is added only when requested.

See [the architecture guide](docs/architecture/APPLICATION-ARCHITECTURE.md) for ownership and dependency rules.
